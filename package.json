{
  "name": "react-extended-render",
  "version": "0.1.2",
  "description": "Extends React's `Component` by passing `props`, `state`, and `context` to the `render` method of a class.",
  "main": "./lib/ExtendedRender.js",
  "scripts": {
    "prepublish": "npm run lint && npm run _build && npm run _test",
    "build": "npm run lint && npm run _build",
    "_build": "babel src --out-dir lib",
    "test": "npm run lint && npm run _test",
    "_test": "echo Congrats! All ZERO test pass.",
    "lint": "eslint src"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/donavon/react-extended-render.git"
  },
  "keywords": [
    "react",
    "render",
    "component"
  ],
  "author": "Donavon West",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/donavon/react-extended-render/issues"
  },
  "homepage": "https://github.com/donavon/react-extended-render#readme",
  "peerDependencies": {
    "react": "^15.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-react": "^6.24.1",
    "eslint": "^3.19.0",
    "eslint-config-airbnb": "^15.0.1",
    "eslint-plugin-import": "^2.3.0",
    "eslint-plugin-jsx-a11y": "^5.0.3",
    "eslint-plugin-react": "^7.0.1",
    "react": "^15.5.4",
    "react-dom": "^15.5.4"
  }
}
